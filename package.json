{
  "name": "notion-cms-test",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build && npm run copy:generated",
    "preview": "vite preview",
    "generate:pages": "node scripts/generatePages.js",
    "copy:generated": "mkdir -p dist/generated && cp -r generated/* dist/generated/ 2>/dev/null || true"
  },
  "devDependencies": {
    "netlify-cli": "^21.5.2",
    "vite": "^6.3.5"
  },
  "dependencies": {
    "@notionhq/client": "^3.1.3",
    "node-fetch": "^3.3.2",
    "dotenv": "^16.3.1"
  }
}
